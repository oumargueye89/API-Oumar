#Pour connaitre la version et etre sûr
git --version

#Pour connaitre le répertoire où nous sommes et ses fichiers
pwd
ls

# Definir ton identité
git config --global user.name "Oumar Gueye"
git config --global user.email "oumargueye77@gmail.com"
git config -l
git config --list
git config user.name
git config user.email

#Création de répertoire 
mkdir projet7

#Pour entrer dans le répertoire puis sortir
cd projet7
cd ..

#Pour initialiser git
git init

#Pour avoir le status global de mon fichier, pour voir ce qui se passe sur mon projet
git status

#Pour commiter le ou les fichiers, on a deux étapes
git diff
git add nom_du_fichier 
git add *
git status
git commit -m "Text_prq_on_commite"

#Pour connaitre, visualiser l'historique des commit
git log
git lg

#La notion de branche, pour travailler en parallèle sur 2 branches
#Création de branch
git branch nom_de_la_branche
#Pour lister les branches disponibles
git branch
#Pour basculer sur une autre branche
git checkout nom_de_la_branche

#site pour choisir des couleurs : https://flatuicolors.com/palette/se

#Pour répatrier le travail sur la branche principal, il y a trois étapes:
git checkout master
#répatrier le travail
git merge projet7
#supprimer la branche
git branch -d projet7

#Pour collaborer avec d'autres personnes sur mon projet, notion de dépot distant : github
# Associer dépot local et dépot distant
#Comment pousser un répertoire existant avec la ligne de commande
git remote add origin https://github.com/OG89/app_P7_OC.git
git remote -v

#Déposer mon projet sur le dépot distant
git push origin master

#Pour ouvrir le fichier
git open nom_du_fichier

#Pour voir les différences aavant et après ou entre deux branches
git diff
git diff master..projet7

#récupérer le travail des autres 
git pull origin master

#Pour copier un répertoire depuis le net
git clone collé le lien github.com (https://github.com/OG89/app_P7_OC.git)

#Effacer et initialiser Git
rm -rf .git
 git init
 git add .
 git commit -m"first message"
 git remote add origin "LINK"
 git push -u origin master
 
#pusher un Nouveau repertoire
echo "# app-projet7-DS-OC" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:oumargueye89/app-projet7-DS-OC.git
git push -u origin main


 #token
 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
 
 